{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd Backend && python manage.py migrate && gunicorn Backend.wsgi:application --bind 0.0.0.0:$PORT",
    "healthcheckPath": "/api/dashboard/",
    "restartPolicyType": "ON_FAILURE"
  }
}
